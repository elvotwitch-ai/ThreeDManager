# Agent Handoffs

This directory holds one short handoff per verified temporary worktree. It is the input to the finalizer; `docs/context/tasks_diary.md` remains the authoritative log after integration onto `main`.

Follow `docs/operations/DEVELOPMENT_WORKFLOW.md`. Do not delete a handoff until the finalizer has integrated and pushed its candidate.
